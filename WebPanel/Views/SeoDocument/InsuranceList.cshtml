@model SeoInsurancePageDto
@{
    ViewData["Title"] = "بیمه نامه ها";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
    int i = 1;
}


<div class="card p-2 mb-4">
    <div class="row">
        <div class="col-md-1 align-middle align-content-center px-4">
            <i class="fa-solid fa-circle-info fs-4 align-middle"></i>
        </div>
        <div class="col-md-11 px-5">
            <div class="d-flex justify-content-between align-items-center">
                <div class="glass-bg px-2 py-1">
                    <p class="fs-6 m-0"> @Model.SeoInfo?.ShipShipName </p>
                </div>
                <div>
                    <p class="fs-6 m-0"> @Model.SeoInfo?.ProductOwnerCompanyCompanyName </p>
                </div>
                <div>
                    <p class="fs-6 m-0">@Model.SeoInfo?.TransportTypeTitle</p>
                </div>
                <div>
                    <p class="fs-6 m-0">شماره بارنامه: @Model.SeoInfo?.BillOfLadingNumber</p>
                </div>
                <div>
                    <p class="fs-6 m-0">کالا: @Model.SeoInfo?.ProductProductName</p>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="col-lg-12">
    <div class="card">
        <div class="card-header pb-2">
            <div class="d-flex justify-content-between">

                <h3 class="title">
                    <a asp-controller="Seo" asp-action="SeoAccessPage" asp-route-seoId="@Model.SeoInfo?.Id"><i class="fa-solid fa-right-to-bracket me-2"></i></a>
                    <i class="icon-frane"></i>بیمه نامه ها
                </h3>

                <div>
                    <a class="btn mx-2 btn-success rounded"
                       onclick="InsuranceShowModal(null, '@Model.SeoInfo.Id');">
                        <i class="icon-plus align-middle"></i>
                        بیمه جدید
                    </a>
                </div>
            </div>

        </div>
        <div class="card-body pt-0">

            <div class="table-responsive mt-4">
                <table class="table table-sm table-bordered table-hover table-striped" id="data-table">
                    <thead class="bg-primary">
                        <tr>
                            <th class="px-1 text-center">#</th>
                            <th class="px-1 text-center">بیمه گر</th>
                            <th class="px-1 text-center">شماره بیمه</th>

                            <th class="px-1 text-center"> بیمه گزار  </th>
                            <th class="px-1 text-center">تاریخ صدور</th>
                            <th class="px-1 text-center">  نوع کالا</th>

                            <th class="px-1 text-center">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>


                        @foreach (var item in Model.List)
                        {
                            <tr>
                                <td class="align-middle px-2 text-center">@(i++)</td>
                                <td class="align-middle px-2 text-center"> @item.Insurer </td>

                                <td class="align-middle px-2 text-center"> @item.PolicyNumber </td>

                                <td class="align-middle px-2 text-center">@item.Policyholder</td>
                                <td class="align-middle px-2 text-center">@item.IssueDateFa </td>

                                <td class="align-middle px-2 text-center">@item.GoodsType </td>



                                <td class="align-middle px-2 text-center">
                                    @if (item.SeoDocument != null)
                                    {
                                        <a class="btn btn-primary px-2 my-2 rounded"
                                           data-bs-toggle="tooltip" data-bs-placement="top" title="@item.SeoDocument.Document?.OriginalFileName"
                                           onclick="DownloadDocFile(event, '@item.SeoDocument.Document.Id')">
                                            <i class="fa-solid fa-download"></i>
                                        </a>
                                    }


                                    <a class="btn btn-warning px-2 btn-sm btn-icon curve" onclick="InsuranceShowModal('@item.Id', '@Model.SeoInfo.Id');"><i class="icon-pencil fs-6 align-middle"></i></a>

                                    <a id="DeleteSeoInsurance@(i)" onclick="ShowQAlert(event,'DeleteSeoInsurance@(i)');" class="btn btn-danger px-2 btn-sm btn-icon curve btn-questions"
                                       asp-action="DeleteSeoInsurance" asp-route-seoInsuranceId="@item.Id">
                                        <i class="icon-trash fs-6 align-middle fa-flip-horizontal"></i>
                                    </a>

                                </td>
                            </tr>
                        }

                    </tbody>
                </table>



            </div>

        </div>

    </div>
</div>

<form asp-action="GetSeoInsuranceDocFileById" asp-controller="SeoDocument"
      method="post" id="FormForDownloadFileId">
    <input type="hidden" id="docIdForDownload" name="docId" />

</form>

<div id="generalmodal" class="modal fade" data-bs-backdrop="static" data-bs-keyboard="false" role="dialog" tabindex="-1">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">ثبت بیمه نامه</h4>
                <button type="button" class="close btn-close rounded border" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="modalbody">

                <partial name="_CreateInsurance" model="new CreateSeoInsuranceDto()" />

            </div>
        </div>
    </div>
</div>

@section script {

    <script>
        $(document).ready(function () {
            //$('.select2').select2();

        });

        function DownloadDocFile(event, docId) {
            event.preventDefault();

            $("#docIdForDownload").val(docId);
            $("#FormForDownloadFileId").submit();
        }


        function InsuranceShowModal(seoInsuranceId, seoId) {
            $.post("@Url.Action("GetCreateSeoInsuranceDtoById", "SeoDocument")", { seoInsuranceId, seoId })
                .done(function (res) {
                    $("#modalbody").html(res);
                    $("#generalmodal").modal('show');
                })
                .fail(function (xhr, status, error) {
                    location.reload();
                });
        }

    </script>
}

